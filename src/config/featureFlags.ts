export const FEATURE_FLAGS = {
    // Toggles whether default menu options are automatically preselected in the Add-ons overlay
    PRESELECT_DEFAULTS: true,

    // Toggles whether the payment summary breaks down GST and Platform Fee as separate items
    SEPARATE_PLATFORM_FEE: true,
};
